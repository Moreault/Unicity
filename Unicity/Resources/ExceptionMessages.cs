namespace ToolBX.Unicity.Resources;

internal static class ExceptionMessages
{
    internal const string CannotIncrementBecauseMaxValue =
        "Cannot increment ID : Type is {0} and the collection's highest ID is {1}";
}
